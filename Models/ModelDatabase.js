'use strict';

const ModelDatabase = {
    findUser(username){
      return new Promise((resolve, reject) => {
          if (username == 'ace68723' && )
          resolve(username);
      })
    },
    getPassword(username){
      return new Promise((resolve, reject) => {
          if (username == 'ace68723' && )
          resolve('helloworld');
      })
    }
}

module.exports = ModelDatabase;
